{
  "version": 2,
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "server/**", "shared/**", "attached_assets/**"],
        "excludeFiles": ["node_modules/**", ".git/**", "client/**", "scripts/**", "script/**", "*.md", "*.sql", ".env*"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server/index.ts"
    }
  ]
}