## Packages
recharts | Dashboard analytics and charts
framer-motion | Smooth animations for page transitions and components
date-fns | Date formatting for orders and timestamps
lucide-react | Icon system (already in base, but confirming usage)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- Auth handled via /api/user (session-based)
- Role-based access control required (customer, driver, admin)
- Layouts must adapt to user role
- Maps are simulated with placeholder inputs for this MVP
